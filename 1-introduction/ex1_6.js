// Elaborar um programa que leia o valor de um jantar. Calcule e informe o valor da taxa do garçom (10%) e o valor total a ser pago.
let dinnerValue = 80;
const waiter = dinnerValue * 0.1;
const finalValue = dinnerValue + waiter

alert(`Valor do Jantar: R$ ${dinnerValue.toFixed(2)}\nTaxa do Garçom: R$ ${waiter.toFixed(2)}\nTotal: R$ ${finalValue.toFixed(2)}`)

// Elaborar um programa que leia a duração de uma viagem em dias e horas. Calcule e informe a duração total da viagem em números de horas.