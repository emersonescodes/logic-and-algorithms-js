// Elaborar um programa que leia um número. Calcule e informe o dobro desse número.

let num = Number(prompt('Type a num: '));
let doubleNum = num * 2;
alert(`O dobro do número é: ${doubleNum}`);

// Elaborar um programa que leia dois números. Calcule e informe a soma desses números.

let num1 = Number(prompt('Type a number:'))
let num2 = Number(prompt('Type a second number:'))

let result = num1 + num2;

alert(`The result of the sum is: ${num1} + ${num2} = ${result}`)